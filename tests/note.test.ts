// Sample
test('adds 1 + 2 to equal 3', () => {
  function sum(a: number, b: number) {
    return a + b;
  }
  expect(sum(1, 2)).toBe(3);
});

// Test Toolbar
test('label === 1 은 exit state를 나타냅니다.', () => {
  
});
